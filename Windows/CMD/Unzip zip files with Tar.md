In Windows CMD use tar:  

```
tar -xf filename.zip -C C:\path\to\destination
```

- Replace `filename.zip` with your ZIP file name.
- Replace `C:\path\to\destination` with the target directory path.
- The `-C` flag specifies the extraction directory.